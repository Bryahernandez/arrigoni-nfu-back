BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[GranelTags] ADD [procesed] BIT NOT NULL CONSTRAINT [GranelTags_procesed_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
