BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[GranelTags] ADD [type] VARCHAR(255) NOT NULL CONSTRAINT [GranelTags_type_df] DEFAULT 'granel';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
