BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Cuts] ADD [cutStateId] INT NOT NULL CONSTRAINT [Cuts_cutStateId_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
